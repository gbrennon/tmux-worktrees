name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: codeberg-tiny
    steps:
      - uses: actions/checkout@v4

      - uses: ./.forgejo/actions/install-dependencies

      - name: Run tests
        run: echo "wip"
